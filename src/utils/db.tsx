import {Data} from '../data/schema';

export const getAllData = () : Array<Data> => {
    return [];
}

/**
 * Return true or false if write was succesful
 */
export const writeNewEntry = (sighting: Data) : Boolean => {
    return true;
}